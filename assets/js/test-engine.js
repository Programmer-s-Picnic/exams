(async()=>{const card=document.getElementById('testCard'),id=new URLSearchParams(location.search).get('id')||'ctet-paper-1-sample-01';let qs=[],answers=[],pos=0,seconds=600,timer;try{const [td,qd]=await Promise.all([DataService.getTests(),DataService.getQuestions()]);const test=td.tests.find(x=>x.id===id);qs=test.questionIds.map(qid=>qd.questions.find(q=>q.id===qid)).filter(Boolean);answers=Array(qs.length).fill(null);seconds=test.durationMinutes*60;card.innerHTML=`<span class="status confirmed">Free diagnostic</span><h1>${test.title}</h1><p>${test.description}</p><ul><li>${qs.length} questions</li><li>${test.durationMinutes} minutes</li><li>No negative marking</li></ul><label><input type="checkbox" id="honest"> I will answer without external help.</label><button class="button full" id="begin">Begin test</button>`;begin.onclick=()=>{if(!honest.checked)return alert('Please confirm before starting.');start()};function start(){timer=setInterval(()=>{seconds--;renderTime();if(seconds<=0)finish()},1000);render()}function renderTime(){const e=document.getElementById('clock');if(e)e.textContent=`${String(Math.floor(seconds/60)).padStart(2,'0')}:${String(seconds%60).padStart(2,'0')}`}function render(){const q=qs[pos];card.innerHTML=`<div class="test-meta"><strong>Question ${pos+1} of ${qs.length}</strong><strong id="clock"></strong></div><div class="progress"><span style="width:${(pos+1)/qs.length*100}%"></span></div><span class="status">${q.subject}</span><h2>${q.question.en}</h2><div class="options">${q.options.en.map((o,i)=>`<button class="option ${answers[pos]===i?'selected':''}" data-i="${i}">${String.fromCharCode(65+i)}. ${o}</button>`).join('')}</div><div class="test-nav"><button class="button secondary" id="prev" ${pos===0?'disabled':''}>Previous</button><button class="button" id="next">${pos===qs.length-1?'Submit':'Next'}</button></div>`;renderTime();card.querySelectorAll('.option').forEach(x=>x.onclick=()=>{answers[pos]=+x.dataset.i;render()});prev.onclick=()=>{pos--;render()};next.onclick=()=>{if(pos<qs.length-1){pos++;render()}else if(confirm('Submit the test?'))finish()}}function finish(){clearInterval(timer);const correct=qs.reduce((n,q,i)=>n+(answers[i]===q.answerIndex),0),pct=Math.round(correct/qs.length*100),level=pct<45?1:pct<75?2:3;localStorage.setItem('he_latest_result',JSON.stringify({testId:id,correct,total:qs.length,percentage:pct,level,createdAt:new Date().toISOString()}));location.href=`../result/?score=${correct}&total=${qs.length}&level=${level}`}}catch(e){card.innerHTML='<div class="empty">The test could not be loaded. The data repository must be publicly readable.</div>'}})();
