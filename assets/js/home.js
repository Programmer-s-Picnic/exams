(async()=>{try{const d=await DataService.getExams();examCards.innerHTML=d.exams.map(e=>`<article class="exam-card"><span class="status ${e.statusClass}">${e.statusLabel}</span><h3>${e.name}</h3><p>${e.summary}</p><footer><span>${e.nextDate||"Date awaited"}</span><a class="text-link" href="exam/?id=${e.id}">View →</a></footer></article>`).join("")}catch{examCards.innerHTML='<div class="empty">Exam data will appear after the data repository is publicly readable.</div>'}try{const d=await DataService.getAnnouncements();announcementList.innerHTML=d.announcements.filter(x=>x.featured).map(x=>`<article class="notice-item"><div class="notice-date">${x.displayDate}</div><div><h3>${x.title}</h3><p>${x.summary}</p></div><a class="text-link" href="${x.url}" target="_blank" rel="noopener">Official notice →</a></article>`).join("")}catch{announcementList.innerHTML='<div class="empty">Official updates are temporarily unavailable.</div>'}})();
