# Himanshu Exams — UP Primary Teacher Preparation

Static GitHub Pages frontend for diagnostic tests, preparation analysis, registration, login and student planning.

The frontend reads public curriculum and test data from `Programmer-s-Picnic/examsdata`. Prototype registrations are stored in the browser until the PHP API is introduced.
